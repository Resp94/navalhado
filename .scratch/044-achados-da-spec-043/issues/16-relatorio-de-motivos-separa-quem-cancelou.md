# 16: Relatório de motivos separa quem cancelou

**What to build:** o relatório de agenda tem um ranking dos motivos de cancelamento do período. A spec 043 começou justamente por um defeito dele: o ranking mistura o cancelamento que a barbearia fez com o que o cliente fez, e o texto de preenchimento gravado quando o cliente não escreve nada (`cancelado pelo cliente`) tende a ocupar o topo sem dizer nada. A spec 043 criou a autoria do cancelamento, mas deixou o relatório de fora e registrou que a separação merecia ticket próprio.

Depois deste ticket, o gerente lê no relatório os motivos reais, separados por quem cancelou.

**Onde foi achado:** notas finais da spec 043, que registram a separação por autoria como desdobramento natural do ticket 06 da spec 043 e pedem ticket próprio.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] O ranking de motivos separa três grupos: cancelado pela barbearia, cancelado pelo cliente e autoria desconhecida
- [x] Cancelamento anterior à spec 043, sem autoria, entra no grupo de autoria desconhecida; nenhum é atribuído a ninguém
- [x] O texto de preenchimento usado quando o cliente não escreve motivo deixa de contar como motivo no ranking; esses cancelamentos continuam contados no total de cancelamentos do cliente
- [x] O texto de preenchimento passa a ter uma definição única; hoje ele é escrito em três pontos do adaptador do Canal do Cliente e mais um no adaptador em memória, e o relatório precisa reconhecer o mesmo texto que o front grava. Onde a definição única mora é decisão do ticket, registrada aqui
- [x] A comparação com o texto de preenchimento usa a mesma normalização que o ranking já aplica, para não depender de caixa ou espaço
- [x] Motivo em branco continua agrupado como hoje
- [x] O ranking continua respeitando o filtro de profissional do relatório
- [x] As demais quebras do relatório de agenda não mudam
- [x] O tipo de retorno do relatório no módulo de relatórios acompanha a mudança, e a tela do relatório exibe os grupos
- [x] A tela segue o design system: grupos distinguidos por texto e selo sutil, nunca por fundo sólido
- [x] pgTAP cobrindo os três grupos, a exclusão do texto de preenchimento, o filtro de profissional e o isolamento por barbearia do relatório
- [x] Teste do adaptador do módulo de relatórios lendo os grupos novos, e teste de tela do cartão de motivos
- [x] `npm run lint`, `npm test` e `npm run build` passam

**Resultado (2026-09-22):**

- **Decisão registrada: definição única do texto de preenchimento mora no front**, `MOTIVO_CANCELAMENTO_PADRAO_CLIENTE` (`src/modules/canal-cliente/types.ts`), exportada e reusada nos 4 pontos que hoje gravam `'Cancelado pelo cliente'`: os 3 do `SupabaseCanalClienteAdapter.ts` (`cancelarAgendamentoPublicoSessao`, `cancelarAgendamentoPorToken` na chamada principal e no fallback) e o 1 do `InMemoryCanalClienteAdapter.ts`. O SQL não pode importar essa constante — ele mantém o literal normalizado (`'cancelado pelo cliente'`) num comentário explícito apontando para a constante do front, como ponto único de manutenção documentado (mudar um lado exige lembrar do outro; não havia forma de compartilhar de verdade entre TypeScript e PL/pgSQL sem infraestrutura nova, fora do escopo do ticket).
- **Núcleo do relatório** (`private.get_schedule_report_core`, `public.get_schedule_report`): nova migration `20260922191302_044_ticket16_relatorio_motivos_separa_autoria.sql`. `cancellation_reasons` passa de array plano para objeto `{shop, customer, desconhecida}`; a CTE `cancellation_reason_agg` agora agrupa por `(reason_group, reason)`, onde `reason_group` vem de `canceled_by` (`'shop'`/`'customer'`/senão `'desconhecida'` — cobre `null`, que é o único valor possível fora dos dois, então nunca inventa uma quarta categoria) e exclui `btrim(lower(cancellation_reason)) = 'cancelado pelo cliente'` antes de agrupar. Top 10 + "outros" passou a ser `partition by reason_group`, preservando a regra por grupo em vez de global. Nenhuma outra quebra do relatório (`status_totals`, `by_origin`, `by_professional`, `heatmap`) foi tocada — todas continuam lendo de `target_appointments`, que só ganhou a coluna `canceled_by` a mais no `select`.
- **Total do cliente não muda**: `status_totals.canceled` conta direto de `target_appointments` (CTE separada, sem passar pela exclusão do texto de preenchimento), então o cancelamento com o texto padrão continua no total — só sai do ranking de motivos.
- **Filtro de profissional e isolamento por tenant inalterados**: `p_professional_id` continua filtrando `target_appointments` antes de qualquer agregação (nenhuma CTE nova ficou de fora do filtro); `tenant_id = p_tenant_id` idem — nenhuma mudança na fronteira de acesso.
- **Tipos e adaptador**: `RelatorioAgendaMotivosCancelamento` (novo tipo, `{shop, customer, desconhecida}: RelatorioAgendaMotivoCancelamento[]`) substitui o array plano em `RelatorioAgenda.cancellation_reasons`. `SupabaseRelatoriosAdapter.toAgendaCancellationReasons` mapeia cada uma das três chaves com a função de lista já existente (renomeada `toAgendaCancellationReasonList`); grupo ausente na resposta (ex.: RPC antiga em cache, ou payload parcial) vira lista vazia em vez de lançar.
- **Tela** (`AgendaMotivosCancelamento.tsx`): cada grupo com cancelamento vira uma seção com um `Badge` `badgeType="subtle"` (nunca sólido) reaproveitando o mesmo rótulo/cor de "quem cancelou" já usado no Painel de Cancelados do Dia (ticket 13 desta spec: Barbearia = `brand`, Cliente = `info`, Desconhecido = `neutral`) — mesmo vocabulário em duas telas diferentes, uma decisão consciente de consistência. Grupo vazio não aparece; estado vazio (os três grupos vazios) mantém a mensagem já existente. CSV de exportação (`AgendaPage.tsx`) achata os três grupos numa lista com a coluna nova "Cancelado por", preservando a ordem shop → customer → desconhecida.
- **Vermelho provado por mutação, banco**: apliquei via `execute_sql` a versão anterior da função (`create or replace` com o corpo de antes do ticket, sem `canceled_by`/agrupamento) e confirmei que `cancellation_reasons` volta a ser array plano (`[]`) em vez do objeto `{shop, customer, desconhecida}` — a mudança de formato por si só já prova que os 47 asserts do pgTAP que esperam o objeto quebrariam (comparação `jsonb` array vs. objeto nunca é igual). Restaurada a função corrigida via `execute_sql`, reconfirmado o objeto de volta.
- **Vermelho provado por mutação, frontend**: troquei os 7 arquivos de produção tocados pela versão de `dev` (`git show dev:...`) e rodei a suíte de `relatorios`/`canal-cliente`/`AgendaPage` — 16 testes falharam (os novos deste ticket, mais os que dependiam do formato antigo). Restaurados os arquivos corrigidos, a suíte volta a 220/220.
- **pgTAP**: estendido `supabase/tests/database/35_relatorio_agenda.test.sql` (não criei arquivo novo — a cobertura é do mesmo `private.get_schedule_report_core` que o arquivo já testa exaustivamente, com o mesmo contexto/fixtures `t07_context`/`t07_fix`; um arquivo novo duplicaria tenant/profissional/serviço à toa). `plan()` de 42 para 47. As 3 asserções antigas de `cancellation_reasons` foram ajustadas para o novo caminho `-> 'desconhecida'` (a fixture existente nunca grava `canceled_by`, então cai toda em "desconhecida" — documentado inline). Bloco novo com fixture própria (`t16_fix`, período isolado 2026-09-05 para não colidir com o teste de "totais zerados" que já usava 2026-09-01) cobrindo: os três grupos separados numa mesma leitura; o texto de preenchimento (`'Cancelado pelo cliente'`) some do ranking mas `status_totals.canceled` continua contando os 4; filtro de profissional zera os três grupos; isolamento por tenant (`tenant_b` sem Agendamento não vê os cancelamentos de `tenant_a` no mesmo período). Rodado via `execute_sql` dentro de `begin;`...`rollback;` (nada persistido) — `ok 47`.
- Testes novos/alterados em TypeScript: `SupabaseRelatoriosAdapter.test.ts` (teste dedicado aos três grupos, incluindo grupo ausente na resposta virando lista vazia, mais os dois testes existentes ajustados ao novo formato); `AgendaMotivosCancelamento.test.tsx` (arquivo novo — não existia teste de tela dedicado a este componente antes, só via `AgendaPage.test.tsx`): 5 casos cobrindo separação em seções, grupo vazio ausente, estado vazio só com os três grupos vazios, selo sem classe `-solid`, e a contagem ao lado do motivo. `RelatoriosRepository.test.ts`, `useAgenda.test.ts` e `AgendaPage.test.tsx` tiveram as fixtures ajustadas ao novo formato de `cancellation_reasons`.
- Suíte completa: 111 arquivos, 1222 testes (+6 deste ticket: 1 no adaptador Supabase, 5 na tela nova). `npx tsc -b`: 0 erros. `npx oxlint`: exit 0 (mesma contagem de avisos pré-existentes da baseline, 49, nenhum novo). `npm run build`: build 0.
- Baseline do banco de desenvolvimento conferida após toda a verificação: `tenants=3, appointments=36, customers=6, professionals=6, services=11, waiting_list=0, blocked_slots=0` — `appointments` subiu de 35 para 36 por causa do encaixe de teste criado e cancelado durante a verificação no navegador do ticket 15 (documentado lá); nenhuma linha nova deste ticket, que só mudou uma função e não escreveu dado nenhum.
