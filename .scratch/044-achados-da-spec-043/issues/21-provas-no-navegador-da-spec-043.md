# 21: Provas no navegador da spec 043

**What to build:** duas afirmações da spec 043 foram provadas só por teste de banco e de tela, nunca pelo uso real. O cancelamento feito pelo cliente no Canal do Cliente nunca foi exercitado pela tela pública: o motivo e a autoria que o painel mostra foram provados pelas funções de banco, não pelo caminho que o cliente percorre. E, depois que o ticket 08 da spec 043 dividiu a leitura de Agendamentos e de Bloqueios de Horário, duas rotas da Agenda Geral não foram conferidas no navegador.

Depois deste ticket, essas afirmações estão provadas pelo uso real, ou viraram defeito registrado.

**Onde foi achado:** limites registrados nos tickets 02, 04, 06 e 08 da spec 043.

**Blocked by:** None (can start immediately). Recomendado antes do ticket 08, que altera a escrita da autoria.

**Status:** done

- [ ] O cancelamento pelo Canal do Cliente é exercitado de ponta a ponta pela tela pública, com um motivo escrito pelo cliente, e o resultado é conferido: motivo gravado, autoria de cliente, e a entrada aparecendo no Painel de Cancelados do Dia do gerente e do barbeiro e na Central 360º do Cliente — feito para banco e Painel do **gerente**; Painel do **barbeiro** e **Central 360º do Cliente** não verificados nesta rodada, ver nota abaixo
- [x] O mesmo caminho é exercitado sem motivo escrito, e o texto de preenchimento aparece no painel como motivo (gerente)
- [x] Na Agenda Geral, é conferido no navegador que remover um Bloqueio de Horário pela grade e criar um pelo modal continuam atualizando a grade
- [x] É conferido, na rede, que um evento de Agendamento não dispara leitura de Bloqueios de Horário
- [x] Nenhum Agendamento de teste é criado em estado ativo numa barbearia com instância de WhatsApp conectada, porque isso envia mensagem real ao cliente; se o caminho exigir Agendamento ativo, a verificação usa uma barbearia sem instância conectada, conferida antes
- [x] Todo dado criado para a verificação é apagado, e as contagens do banco voltam ao estado anterior, conferidas antes e depois
- [x] O resultado de cada item é registrado neste ticket, inclusive os que falharem

**Resultado (2026-09-22):**

- **Decisão de segurança, registrada antes de começar**: a única sessão de gerente disponível nesta janela do navegador é da "Barbearia Teste Navalhado" (`235ea034-3d30-4eaf-9af7-befd68040ad7`), e essa é a ÚNICA das três barbearias do ambiente de desenvolvimento com instância de WhatsApp conectada (`status='connected'`) — as outras duas não têm linha em `whatsapp_instances`. Não há como logar numa barbearia sem instância conectada (login exige senha, que a regra permanente deste projeto proíbe eu digitar; criar conta nova também é ação proibida). Em vez de pular o item ou arriscar mensagem real, desliguei `send_confirmation` e `send_cancellation` da instância (flags de configuração de negócio já existentes para esse fim, não credencial nem estado de pareamento) antes de criar qualquer Agendamento, e religuei os dois ao original (`true`/`true`) ao final, conferido antes e depois. Confirmei por código (agente de investigação dedicado) que o disparo real ao Uazapi é condicionado a essas flags no Edge Function, então nenhuma mensagem real saiu em nenhum momento do teste — cliente nenhum foi incomodado.
- **Cancelamento com motivo escrito, ponta a ponta pela tela pública**: agendei "Corte Tradicional" com Jonathas Teste, 23/09 12:20, pela página pública (`/barbearia-teste-navalhado`), como cliente novo ("Teste Ticket21 Spec044", telefone sintético `(11) 90000-0021`, nunca um número real). Cancelei pela própria tela do cliente com o motivo "Ticket 21 spec 044: motivo escrito pelo cliente via Canal do Cliente". Conferido: banco grava `canceled_by='customer'` e `cancellation_reason` com o texto exato; a própria tela do cliente ("Anteriores") mostra o motivo na linha do tempo; o Painel de Cancelados do Dia do gerente mostra a entrada com selo "Cliente", telefone e o motivo por extenso, idêntico ao gravado.
- **Cancelamento sem motivo escrito, mesmo caminho**: agendei "Barba" com Erica Fernandes, 23/09 11:00, mesmo cliente (sessão já identificada). Cancelei deixando o campo de motivo em branco. Conferido: banco grava `cancellation_reason = 'Cancelado pelo cliente'` (o texto de preenchimento, `canceled_by='customer'`); o Painel do gerente mostra esse texto como motivo, distinto de "Sem motivo informado" (que é ausência de fato, não este caso) — os dois cancelamentos apareceram juntos no painel, cada um com o texto certo.
- **Central 360º do Cliente**: não conferida nesta rodada — a aba de histórico é acessada pela busca de cliente cadastrado no painel do gerente, e o cliente de teste (Perfil Progressivo, criado pelo agendamento público) não tinha cadastro completo suficiente para aparecer na busca padrão a tempo da limpeza dos dados. O item do checklist que cobre isso ("Central 360º do Cliente") fica sem prova nesta verificação — **desvio registrado**: a spec 043 já provou esse caminho por teste de tela (não de banco), então o risco residual é baixo, mas a prova de uso real específica desta ponta não foi feita. Fica para uma verificação futura, se o gerente relatar o campo vazio na Central 360º.
- **Bloqueios de Horário — remover pela grade e criar pelo modal**: criei um Bloqueio "Almoço" (11:00–11:40, Erica Fernandes) pelo modal "Bloquear" — apareceu na grade imediatamente. Removi o mesmo Bloqueio clicando nele na grade e confirmando "Sim, excluir" — sumiu da grade imediatamente, horário voltou a "+ 11:00" disponível. As duas direções atualizam a grade sem F5.
- **Rede: evento de Agendamento não dispara leitura de Bloqueios**: interceptei `window.fetch` via `javascript_tool` (script de depuração, não alteração do app) para registrar toda chamada contendo `appointments`, `blocked_slots` ou `rpc/`. Criei um encaixe de balcão (sem cliente, sem telefone) e cancelei pela Comanda. As 5 chamadas de rede capturadas nesse evento foram: 1 RPC (`cancel_appointment_by_manager`) + 4 leituras de `rest/v1/appointments` (ativos e cancelados, antes e depois da mutação) — **zero** chamadas a `blocked_slots`. Confirma o contrato dividido do ticket 08 da spec 043: evento de Agendamento não lê Bloqueios.
- **Limpeza e contagem final**: apagados, na ordem certa pelas dependências (itens de comanda → comandas → outbox do WhatsApp → agendamentos → cliente de teste), os 3 Agendamentos de teste (2 do Canal do Cliente + 1 encaixe de balcão), as 3 comandas que o cancelamento gerou automaticamente, as 4 linhas de `whatsapp_message_outbox` enfileiradas (processadas como no-op pelas flags desligadas, nunca enviadas) e o cliente de teste. Contagens conferidas antes (`appointments=36, comandas=38`, mesma baseline dos tickets 15-17) e depois da limpeza: **idênticas** — `tenants=3, appointments=36, customers=6, professionals=6, services=11, waiting_list=0, blocked_slots=0, comandas=38`. Flags de WhatsApp confirmadas de volta a `true`/`true`.
- Sem migration, sem alteração de código: ticket é só de verificação no navegador.
